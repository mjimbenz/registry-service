spring:
  application:
    name: registry-service   # ESTE NOMBRE debe coincidir con el nombre del archivo en config-repo

  config:
    import: "configserver:http://config-server:8888"

  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
